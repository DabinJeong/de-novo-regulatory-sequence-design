# De novo sequence generation

## Installation
The environment required to run {our model} can be installed like so:

Install the project

- You can install the requirements with [poetry](https://github.com/python-poetry/poetry):  
  `poetry install`
